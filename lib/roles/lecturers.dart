// Lecturers Functionalities

import 'package:flutter/material.dart';

class LecturerPage extends StatelessWidget {
  const LecturerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Lecturer Dashboard')),
      body: const Center(child: Text('Lecturer functionalities go here')),
    );
  }
}
